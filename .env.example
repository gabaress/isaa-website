# Demo builds only: renders unsupplied content as visible, dashed placeholders.
# Leave unset everywhere else — the default hides them. See CONTENT.md.
NEXT_PUBLIC_SHOW_PLACEHOLDERS=true

# Set on Vercel once isaa.archery.ie resolves; until then the deployment URL is used.
# NEXT_PUBLIC_SITE_URL=https://isaa.archery.ie
